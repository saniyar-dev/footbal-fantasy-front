import React, {FC, ReactElement} from "react";
import styled from "styled-components";

const Container = styled.div`
    display: flex;
`

const PlayerPreview: FC = (): ReactElement => {
    return <Container></Container>
}

export default PlayerPreview
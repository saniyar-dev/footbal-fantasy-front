import Column from '@src/atomComponents/Grid/Column'
import React, {FC, ReactElement} from 'react'

const RewardsPage: FC = (): ReactElement => {
    return <Column>

    </Column>
}

export default RewardsPage